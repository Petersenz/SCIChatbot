# Sidebar refinement (26 September 2026)
Only new-chat icon changed to the former FiEdit: all existing button sizing, colors, handlers and flow motion preserved. Staff access is now a quiet account row with icon tile and chevron, distinct from primary new-chat. Guest login uses prefetch, 160ms exit fade and 380ms login entry; reduced motion skips exit delay/animation. Modified-click/native anchor preserved, authenticated staff retain full navigation to preserve auth bootstrap. Timers cleaned on unmount and stalled navigation restores controls. No auth API/roles/session changes.
Menu uses restrained shadow, smaller icons/text and subtle divider; 44px mobile targets, keyboard and portal targeting retained. No backend/RAG/data changes.
Validation: production build+TypeScript and diff check pass; preview guest login route and browser-back pass, existing chat history intact, menu keyboard/Escape pass. Sidebar 320/375/414/768 has no horizontal overflow; console errors0. No authenticated login submission or destructive action tested. Build first failed due missing Node PATH and cached Turbopack failure, repaired build environment and renamed only workspace .next for clean rebuild; production unaffected.
Deployment: baseline520d92a, immutable release/atomic symlink, only SCI web restart after idle, previous release rollback retained. Backend code identical. No Nginx or shared data mutation. Project state records final live verification.
